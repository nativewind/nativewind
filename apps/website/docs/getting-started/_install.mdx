import NPM from "../_npm.mdx";

You will need to install `nativewind` and it's peer dependencies `tailwindcss` and `react-native-reanimated`.

<NPM
  deps={["nativewind", "react-native-reanimated"]}
  devDeps={["tailwindcss"]}
/>
