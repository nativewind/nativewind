import NPM from "../_npm.mdx";

You will need to install `nativewind@^4.0.36` and it's peer dependencies `tailwindcss` and `react-native-reanimated`.

<NPM
  deps={["nativewind@^4.0.36", "react-native-reanimated", "tailwindcss"]}
/>

Run `pod-install` to install Reanimated pod:

```bash
npx pod-install
```
