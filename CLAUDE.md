# Claude Code Context

Read these files for project context:

- @DEVELOPMENT.md — architecture, directory structure, commands, testing, pitfalls
- @contributing.md — contribution workflow, PR process, changesets

## Pre-push Checks

Before pushing any commits, always run `npx prettier --check` on changed `.ts`/`.tsx` files and fix any formatting issues before pushing.
