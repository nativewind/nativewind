---
"nativewind": patch
"react-native-css-interop": patch
---

Support the Expo SDK 57 toolchain while preserving the Nativewind v4 Tailwind 3 configuration and public entry points.

Keep Reanimated hooks in a stable component when animations are added after mount. Handle the React Native Appearance unspecified value, fix box shadow compiler fallthrough, update Metro transformer types, and select the Reanimated plugin entry that supports both Reanimated 3 and Reanimated 4.

Update the source test environment to React 19 and React Native 0.86. Add focused regression tests and retain the existing consumer peer minimums. No release candidate is required.
